services:
  fragrag-backend:
    image: krishnarajt/fragrag-backend
    container_name: fragrag-backend
    ports:
      - "8000:8000"
    restart: unless-stopped
    networks:
      - fragrag-network

networks:
  fragrag-network:
    driver: bridge